# Spec: 병록번호·접수일자 추출

## 목적
Master DB에서 병록번호와 접수일자를 추출하여 외부 시스템에 R-ID를 요청하기 위한 파일을 생성합니다.

## 입력
- `Master_DB.xlsx` (통합데이터 시트)

## 처리 순서
1. 통합데이터 시트에서 병록번호·접수일자 컬럼 추출
2. 중복 제거
3. 병록번호 텍스트 서식 적용

## 출력
`병록번호_접수일자_추출.xlsx` — 입력 파일과 동일 폴더 저장
- 헤더 없음 (외부 시스템 제출 포맷)
- 병록번호 컬럼 텍스트 서식
